# RVST for VS Code

Syntax highlighting for `<rust>` blocks in Svelte files and rust-analyzer integration for RVST native code.

## Features
- Rust syntax highlighting inside `<rust>` blocks in `.svelte` files
- Automatic rust-analyzer configuration for `src-native/` Cargo projects
